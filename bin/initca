#!/bin/sh
#
# Generates CA cert/key and single key/cert.
#
[ "$#" -eq 1 ] || { echo "Usage: $0 host" >&2; exit 1 }
HOST="$1"
echo "Generating CA key/cert in /certs.."
mkdir -p /certs/
cfssl gencert \
  -initca /etc/configs/ca-csr.json | cfssljson -bare /certs/ca
